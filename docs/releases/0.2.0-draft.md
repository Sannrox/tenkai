# Release notes draft: v0.2.0

Status: **draft for maintainer go/no-go**. Not tagged. Crate remains `0.1.0`
until an authorized version bump and tag.

Pair with [release readiness](../release-readiness.md) and the
`prepare-release` skill. Target commit: tip of `main` after multi-replica hub
runbook (#130 / PR #134).

## Scope

Recommended SemVer: **`0.2.0`** (pre-1.0 minor = large additive surface since
the initial public tree; no claim of 1.0 stability).

Base: first public commits on this repository through architecture ADRs
0001–0009 and product depth through staged products, Postgres hub store, and
multi-replica fencing.

Open release-blocking issues: **none** (empty open backlog at draft time).

## Compatibility and migration

| Surface | Impact for 0.2 |
| --- | --- |
| Crate version | Bump `0.1.0` → `0.2.0` only on authorized tag commit |
| SQLite `SCHEMA_VERSION` | Unchanged at draft time; community path remains single-writer |
| Runtime / auth / federated / model / plan / catalog contracts | Still at documented `*_CONTRACT_VERSION` / `PLAN_FORMAT_VERSION` = 1 unless a later PR says otherwise |
| Optional Postgres hub | Feature `postgres`; `TENKAI_POSTGRES_URL`; schema-per-tenant; not the community default |
| Multi-replica | `--replica-count > 1` requires store `shared_replica_state`; tick fencing is optional wiring |
| `high_availability` capability | **Still not claimed** by Postgres adapter; flag remains fail-closed without it |
| Defaults | Signing/approval fail closed; tenant mode / enterprise flags default off |

### Operator upgrade notes

1. **Embedded / SQLite:** rebuild binaries; existing `.tenkai-state` continues
   if schema version still matches. Run `tenkaictl backup` before major upgrades.
2. **Optional multi-tenant hub:** enable `postgres`, set `TENKAI_POSTGRES_URL`,
   follow [postgres-tenant-store.md](../postgres-tenant-store.md). Do not
   co-locate IdP databases.
3. **Multi-replica hubs:** follow
   [multi-replica-hub-runbook.md](../multi-replica-hub-runbook.md). Prefer
   single active writer + shared fence; do not share community SQLite files.
4. **Development bypasses** (`--allow-unsigned-development`,
   `--allow-unapproved-development`) remain explicit and audited — not
   production defaults.

### Rollback

Previous binary + restore of Tenkai-owned SQLite backup (embedded) or Postgres
`pg_restore` (hub), then re-inject tokens from the secret store. Recovery never
depends on sekai-chisei.

## User-visible themes (classify for notes)

### Added

- Fleet status, fleet drift watch, multi-environment rollout waves
- Environment capability facts + local hardware inventory probe
- Helm and native Kubernetes (kubectl) software apply paths
- Model runtime control plane, llama.cpp reference path, canary × model_runtime
- Staged products: `policy_bundle`, `eval_suite`, `agent_definition`
- Optional advisory plan priors (local file)
- Remote HTTP GateProvider for chisei-compatible eval decisions
- AuthStack management HTTP, federated identity accept path, JWT EdDSA assertion verifier
- Tenant isolation on management HTTP surfaces; in-memory and optional Postgres tenant stores
- Postgres hub `shared_replica_state` (single-active-writer) and multi-host tick fencing
- Multi-replica hub ops runbook; ADR 0008 / 0009

### Changed

- README Status table tracks second-gen fleet, hub, and staged-product surface
- Capability negotiation remains fail-closed for multi-replica / HA / tenant flags

### Security

- Fail-closed signing, plan approval, tenant non-disclosure, and capability gates preserved
- Diagnostics and fleet baselines must not carry management/runtime tokens

### Migration

- Optional only: Postgres feature and multi-replica server flags; community path unchanged

## Validation (run on tag commit)

```bash
cargo fmt --check
cargo test --locked
cargo clippy --all-targets --locked -- -D warnings
cargo build --all-targets --locked
```

Confirm GitHub required checks green on the release commit.

## Go / no-go

| Gate | Draft status |
| --- | --- |
| Required checks on release commit | Run at tag time |
| No open release-blocking issues | Yes at draft time |
| Upgrade / security notes written | This document |
| Maintainer authorizes version bump + tag + GitHub Release | **Pending** |

**Recommendation:** `go` for content readiness after checklist run on tip of
`main`; tag only with explicit maintainer authorization.

## Out of scope for 0.2

- Advertising product `high_availability` capability
- Durable DB-backed tick fence (implementation follow-on)
- OutcomeProvider → plan priors projection
- Runtime-agent inventory heartbeat
- Hub OpenMetrics scrape endpoint
- Catalog service extraction
