[product]
name = "replay-service"
version = "0.1.0"
description = "Healthy baseline for the recorded rollback incident"

[deploy]
workdir = "."
install = "mkdir -p .state && printf '0.1.0\\n' > .state/version"
health = "test \"$(cat .state/version)\" = '0.1.0'"
uninstall = "rm -rf .state"
