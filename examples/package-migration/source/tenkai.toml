[product]
name = "pkg"
version = "1.0.0"
description = "Synthetic source package for a governed migration"

[deploy]
install = "sh ./target.sh apply"
health = "sh ./target.sh health"
uninstall = "sh ./target.sh remove"
inputs = ["payload.txt", "target.sh"]
