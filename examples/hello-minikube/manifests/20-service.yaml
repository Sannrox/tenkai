apiVersion: v1
kind: Service
metadata:
  name: hello-minikube
  labels:
    app: hello-minikube
spec:
  selector:
    app: hello-minikube
  ports:
    - name: http
      port: 80
      targetPort: 80
